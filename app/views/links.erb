
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Links</title>
  </head>
  <body>

    <h1> Links </h1>
    <ul id='links'>
      <% @links.each do |link| %>
        <li>
          Title:  <%= link.title %>
          URL:    <a href="<%= link.url %>"><%= link.url %></a>
          Tag:    <% link.tags.each do |tag| %>
                   <%= tag.tag_name %>
                  <% end %>
        </li>
      <% end %>
    </ul>


    <a href="links/new"><button type="button">Add Link</button></a>

  </body>
</html>
